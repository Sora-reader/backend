[tool.poetry]
name = "reader-tbd-backend"
version = "0.1.0"
description = ""
authors = ["Egor Ena <egorena@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
uvicorn = "^0.13.3"
SQLAlchemy = "^1.3.22"
fastapi-users = {extras = ["sqlalchemy"], version = "^5.0.0"}
fastapi = "^0.63.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"